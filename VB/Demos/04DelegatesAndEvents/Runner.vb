Namespace DelegatesAndEvents
  Module Runner
    Sub RunMe()
      Dim client = New ConsoleClient()
      client.Print()

      'Dim winClient = New WpfClient()
      'winClient.ShowDialog()
    End Sub
  End Module
End Namespace

